package day04;

public class Singer {
    String singerName;

    /**
     * 매개 변수가 없는 생성자를 기본 생성자
     * 클래스 안에서 생성자가 하나도 없을 경우 자동 생성
     *
     * @param singerName
     */
    Singer(String singerName) {
        this.singerName = singerName;
    }

    void hello() {
        double g = 2.2;
        for (int i = 0; i < Integer.parseInt(new String("13")); i++) {

        }
    }

}
